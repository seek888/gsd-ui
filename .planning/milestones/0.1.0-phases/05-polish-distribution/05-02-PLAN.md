---
phase: 05-polish-distribution
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/stores/uiStore.ts, src/lib/store.ts, src/App.tsx]
autonomous: true
requirements: []
must_haves:
  truths:
    - "项目路径在应用重启后自动恢复"
    - "侧边栏折叠状态在应用重启后保持"
    - "当前视图（Dashboard/Roadmap/Terminal/Documents）在应用重启后恢复"
    - "所有设置通过 @tauri-apps/plugin-store 持久化到 gsd-ui-settings.json"
  artifacts:
    - path: "src/stores/uiStore.ts"
      provides: "UI 状态持久化（侧边栏、当前视图）"
      exports: ["loadSettings", "saveSettings"]
    - path: "src/lib/store.ts"
      provides: "Tauri store 插件封装"
      exports: ["getStore", "getSetting", "setSetting"]
    - path: "src/App.tsx"
      provides: "应用启动时加载设置"
  key_links:
    - from: "src/App.tsx"
      to: "src/stores/uiStore.ts"
      via: "useEffect 调用 loadSettings"
      pattern: "useEffect.*loadSettings"
    - from: "src/stores/uiStore.ts"
      to: "src/lib/store.ts"
      via: "调用 getSetting/setSetting"
      pattern: "getSetting|setSetting"
---

<objective>
扩展设置持久化功能，确保所有 UI 偏好在应用重启后保持

**目的**: 根据 ROADMAP.md Success Criteria #2，持久化项目路径和 UI 偏好
**输出**: 扩展的 uiStore（包含持久化）、App 启动时的设置加载逻辑
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish-distribution/05-CONTEXT.md
@src/stores/uiStore.ts
@src/stores/projectStore.ts
@src/lib/store.ts
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 uiStore 添加持久化功能</name>
  <files>src/stores/uiStore.ts</files>
  <read_first>
    - src/stores/uiStore.ts (当前 UI store 实现)
    - src/stores/projectStore.ts (参考 loadSettings 模式)
    - src/lib/store.ts (了解 store API)
  </read_first>
  <action>
修改 `src/stores/uiStore.ts` 添加持久化功能：

**当前接口** (保持向后兼容):
```typescript
interface UIState {
  activeView: ActiveView;
  sidebarCollapsed: boolean;
  setActiveView: (view: ActiveView) => void;
  toggleSidebar: () => void;
}
```

**扩展后添加**:
```typescript
interface UIState {
  // 现有字段...
  loadSettings: () => Promise<void>;
  saveSettings: () => Promise<void>;
}
```

**实现细节**:
1. `loadSettings`:
   - 调用 `getSetting<ActiveView>('activeView', 'dashboard')`
   - 调用 `getSetting<boolean>('sidebarCollapsed', false)`
   - 使用 set() 更新 store 状态

2. `saveSettings`:
   - 调用 `setSetting('activeView', activeView)`
   - 调用 `setSetting('sidebarCollapsed', sidebarCollapsed)`

3. 修改 `setActiveView` 和 `toggleSidebar`:
   - 在状态更新后调用 `saveSettings()`
   - 确保每次 UI 变更都自动持久化

4. 添加错误处理:
   - 捕获 store 操作的异常
   - 失败时记录到 console.error（但不阻塞 UI）
  </action>
  <verify>
  <automated>npx tsc --noEmit && grep -n "loadSettings\|saveSettings" src/stores/uiStore.ts | wc -l | grep -q "4"</automated>
  </verify>
  <done>
- uiStore.ts 包含 loadSettings 和 saveSettings 方法
- setActiveView 和 toggleSidebar 调用 saveSettings
- 编译无 TypeScript 错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 在 App 启动时加载 UI 设置</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (了解当前应用启动流程)
    - src/stores/projectStore.ts (参考现有的 loadSettings 调用)
  </read_first>
  <action>
修改 `src/App.tsx` 添加 UI 设置加载：

**当前流程** (保持):
- projectStore.loadSettings() 已在某个地方调用

**新增**:
1. 导入 `useUIStore`:
   ```typescript
   import { useUIStore } from '@/stores/uiStore';
   ```

2. 在 App 组件中添加 useEffect:
   ```typescript
   const loadUISettings = useUIStore(state => state.loadSettings);

   useEffect(() => {
     loadUISettings();
   }, [loadUISettings]);
   ```

3. 确保加载顺序:
   - 先加载 projectStore (可能已有)
   - 再加载 uiStore (新增)
   - 并行加载以提高启动速度（使用 Promise.all）

4. 添加加载状态管理:
   - 如果需要，可以在 store 中添加 `isInitialLoad` 状态
   - 或者在 App 中使用本地 useState 跟踪加载状态
  </action>
  <verify>
  <automated>npx tsc --noEmit && grep -n "useUIStore\|loadUISettings" src/App.tsx | wc -l | grep -q "2"</automated>
  </verify>
  <done>
- App.tsx 导入并使用 useUIStore
- App 启动时调用 loadUISettings
- 编译无 TypeScript 错误
  </done>
</task>

<task type="auto">
  <name>Task 3: 验证持久化键值和默认值</name>
  <files>src/lib/store.ts</files>
  <read_first>
    - src/lib/store.ts (确保 store 封装支持所需类型)
  </read_first>
  <action>
验证 `src/lib/store.ts` 支持所需的类型和键值：

**检查点**:
1. `getSetting<T>` 泛型函数支持 ActiveView 类型
2. `setSetting<T>` 正确保存复杂类型
3. Store 文件名为 `gsd-ui-settings.json`

**如果需要修改**:
- 确保类型定义正确
- 添加 JSDoc 注释说明每个设置的用途

**持久化键值文档** (在代码中添加注释):
```typescript
// Settings keys stored in gsd-ui-settings.json:
// - projectPath: string | null (from projectStore)
// - activeView: 'dashboard' | 'roadmap' | 'terminal' | 'documents' (from uiStore)
// - sidebarCollapsed: boolean (from uiStore)
```

这些设置在应用重启后自动恢复，满足 ROADMAP.md Success Criteria #2。
  </action>
  <verify>
  <automated>grep -n "gsd-ui-settings.json\|getSetting\|setSetting" src/lib/store.ts | wc -l | grep -q "3"</automated>
  </verify>
  <done>
- store.ts 包含正确的泛型支持
- 设置键值在代码中有文档注释
- 所有持久化设置在应用重启后恢复
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings Store → App | 设置文件可能被外部修改 |
| UI → Settings Store | 用户输入可能包含非预期值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | T (Tampering) | Settings file | accept | 设置仅影响 UI 偏好，不影响安全逻辑 |
| T-05-02-02 | I (Information Disclosure) | Settings file | accept | 设置文件仅包含路径和 UI 状态，无敏感信息 |
| T-05-02-03 | D (Denial of Service) | Settings loading | mitigate | 加载失败时使用默认值，不阻塞应用启动 |
</threat_model>

<verification>
1. 检查 TypeScript 编译: `npx tsc --noEmit`
2. 验证设置文件: 运行应用后检查 `~/Library/Application Support/com.gsd.ui/gsd-ui-settings.json` (macOS) 或 `%APPDATA%/com.gsd.ui/gsd-ui-settings.json` (Windows)
3. 测试持久化: 更改设置后重启应用，验证设置保持
</verification>

<success_criteria>
1. 项目路径在重启后恢复
2. 侧边栏状态在重启后恢复
3. 当前视图在重启后恢复
4. 设置加载失败时应用仍能启动（使用默认值）
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-distribution/05-02-SUMMARY.md`
</output>
