---
phase: 05-polish-distribution
plan: 05
type: execute
wave: 4
depends_on: [05-03, 05-04]
files_modified: [src-tauri/tauri.conf.json, src-tauri/icons/]
autonomous: true
requirements: []
must_haves:
  truths:
    - "应用在标题栏显示正确的名称和版本"
    - "应用图标在 Dock/任务栏正确显示"
    - "应用元数据（描述、类别、发布者）已配置"
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "应用元数据配置"
      contains: "productName, version, identifier, category"
    - path: "src-tauri/icons/"
      provides: "应用图标资源"
      min_files: 5
  key_links:
    - from: "tauri.conf.json"
      to: "src-tauri/icons/"
      via: "bundle.icon 配置引用图标文件"
      pattern: "icons/"
---

<objective>
配置应用图标和元数据

**目的**: 根据 ROADMAP.md Success Criteria #5，为应用提供专业的外观和标识
**输出**: 完整的应用图标集和元数据配置
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish-distribution/05-CONTEXT.md
@src-tauri/tauri.conf.json
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证和更新应用元数据</name>
  <files>src-tauri/tauri.conf.json</files>
  <read_first>
    - src-tauri/tauri.conf.json (当前元数据配置)
  </read_first>
  <action>
验证和更新 `src-tauri/tauri.conf.json` 中的应用元数据：

**必填字段** (根据 ROADMAP.md):
1. `productName`: "GSD UI" 或 "GSD"
2. `version`: "0.1.0" (遵循语义化版本)
3. `identifier`: "com.gsd.ui" (反向域名格式)
4. `bundle.category`: "public.app-category.developer-tools" (macOS) / "Developer Tool" (通用)
5. `bundle.shortDescription`: 简短描述（一句话）
6. `bundle.longDescription`: 详细描述（1-2 段）
7. `bundle.publisher`: "gsd-ui" 或组织名称

**推荐配置**:
```json
{
  "productName": "GSD UI",
  "version": "0.1.0",
  "identifier": "com.gsd.ui",
  "bundle": {
    "category": "public.app-category.developer-tools",
    "shortDescription": "Visual interface for the GSD workflow",
    "longDescription": "GSD UI provides a desktop interface for the GSD (Get Shit Done) workflow. Manage projects, execute commands, and edit planning documents in one app.",
    "publisher": "GSD UI"
  }
}
```

**Windows 特定元数据**:
```json
{
  "bundle": {
    "windows": {
      "wix": {
        "language": "en-US"
      }
    }
  }
}
```

更新配置使其包含完整的应用元数据。
  </action>
  <verify>
  <automated>cat src-tauri/tauri.conf.json | jq -r '.productName, .version, .identifier, .bundle.shortDescription' | grep -v "null"</automated>
  </verify>
  <done>
- tauri.conf.json 包含所有必填元数据字段
- productName、version、identifier 设置正确
- shortDescription 和 longDescription 已填写
- publisher 已设置
  </done>
</task>

<task type="auto">
  <name>Task 2: 验证应用图标资源</name>
  <files>src-tauri/icons/</files>
  <read_first>
    - src-tauri/tauri.conf.json (图标配置路径)
  </read_first>
  <action>
验证 `src-tauri/icons/` 目录中的应用图标：

**必需的图标文件** (根据 Tauri 文档):
1. `32x32.png` - 32x32 像素 PNG
2. `128x128.png` - 128x128 像素 PNG
3. `128x128@2x.png` - 256x256 像素 PNG (Retina)
4. `icon.icns` - macOS 图标容器
5. `icon.ico` - Windows 图标容器

**检查图标配置**:
```bash
ls -la src-tauri/icons/
```

**图标设计决策** (Claude's discretion - D-Deferred):
- **当前状态**: 使用默认 Tauri 图标
- **自定义图标**: 需要设计并生成图标集
- **临时方案**: 使用默认图标（功能完整）

**如果需要更新图标**:
1. 设计 1024x1024 原始图标
2. 生成各种尺寸的 PNG
3. 使用图标工具生成 .icns 和 .ico:
   - macOS: `iconutil -c icns icon.iconset`
   - Windows: 使用 ImageMagick 或在线工具
4. 替换 `src-tauri/icons/` 中的文件

**验证图标引用**:
```json
{
  "bundle": {
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
  }
}
```

确认所有图标文件存在且配置正确引用它们。
  </action>
  <verify>
  <automated>ls -1 src-tauri/icons/ | wc -l | grep -q "5"</automated>
  </verify>
  <done>
- src-tauri/icons/ 包含所有必需的图标文件
- 图标文件尺寸正确
- tauri.conf.json 正确引用图标路径
  </done>
</task>

<task type="auto">
  <name>Task 3: 验证窗口标题和显示配置</name>
  <files>src-tauri/tauri.conf.json</files>
  <read_first>
    - src-tauri/tauri.conf.json (窗口配置)
  </read_first>
  <action>
验证 `src-tauri/tauri.conf.json` 中的窗口显示配置：

**窗口标题配置**:
```json
{
  "app": {
    "windows": [
      {
        "title": "GSD UI",
        "width": 1200,
        "height": 800,
        "minWidth": 900,
        "minHeight": 600,
        "resizable": true,
        "fullscreen": false,
        "center": true,
        "decorations": true,
        "transparent": false
      }
    ]
  }
}
```

**检查项**:
1. `title` 与 `productName` 一致
2. `decorations: true` (显示标准窗口边框和标题栏)
3. `transparent: false` (不透明窗口，性能更好)
4. `center: true` (窗口居中显示)
5. `minWidth/minHeight` 合理（防止窗口太小）

**安全配置**:
```json
{
  "app": {
    "withGlobalTauri": false,  // 生产环境应设为 false
    "security": {
      "csp": "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline';"
    }
  }
}
```

**生产构建配置**:
```json
{
  "bundle": {
    "active": true,
    "targets": "all"
  },
  "build": {
    "beforeDevCommand": "npm run dev",
    "beforeBuildCommand": "npm run build",
    "frontendDist": "../dist"
  }
}
```

更新配置确保窗口标题和显示正确。
  </action>
  <verify>
  <automated>cat src-tauri/tauri.conf.json | jq '.app.windows[0].title' | grep -q "GSD"</automated>
  </verify>
  <done>
- 窗口标题设置为 "GSD UI"
- 窗口尺寸和约束配置合理
- CSP 安全策略配置正确
- 生产构建配置正确
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App metadata → OS | 应用名称、图标、类别注册到系统 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | S (Spoofing) | App name/icon | accept | 用户选择安装，手动验证来源 |
| T-05-05-02 | T (Tampering) | Metadata | accept | 元数据不包含敏感信息 |
| T-05-05-03 | I (Information Disclosure) | Metadata | accept | 元数据包含公开信息 |
| T-05-05-04 | D (Denial of Service) | CSP | mitigate | CSP 限制资源加载，防止 XSS |
</threat_model>

<verification>
1. 验证元数据: `cat src-tauri/tauri.conf.json | jq '.productName, .version, .identifier'`
2. 验证图标存在: `ls -1 src-tauri/icons/`
3. 验证窗口配置: `cat src-tauri/tauri.conf.json | jq '.app.windows[0]'`
4. 重新构建验证: `npm run tauri:build` 并检查生成的应用
</verification>

<success_criteria>
1. 应用元数据完整且正确
2. 图标文件存在且配置正确
3. 窗口标题和显示配置合理
4. 应用在 Dock/任务栏显示正确的名称和图标
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-distribution/05-05-SUMMARY.md`
</output>
