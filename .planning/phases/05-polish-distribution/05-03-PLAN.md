---
phase: 05-polish-distribution
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [src-tauri/tauri.conf.json, package.json]
autonomous: false
requirements: []
must_haves:
  truths:
    - "macOS .app bundle 成功构建"
    - ".app 可以双击启动并正常运行"
    - "所有核心功能在 macOS 上正常工作（CLI 检测、命令执行、文件浏览）"
  artifacts:
    - path: "src-tauri/target/release/bundle/macos/GSD UI.app"
      provides: "macOS 可执行应用包"
      min_lines: 1
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri 配置（macOS 构建）"
      contains: "macOS"
  key_links:
    - from: "npm run tauri:build"
      to: "src-tauri/target/release/bundle/macos/GSD UI.app"
      via: "Tauri CLI 构建流程"
      pattern: "tauri build"
---

<objective>
构建和测试 macOS .app bundle

**目的**: 根据 ROADMAP.md Success Criteria #3，确保应用在 macOS 上正确构建和运行
**输出**: 可分发的 macOS .app bundle
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish-distribution/05-CONTEXT.md
@src-tauri/tauri.conf.json
@package.json
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证 macOS 构建配置</name>
  <files>src-tauri/tauri.conf.json, package.json</files>
  <read_first>
    - src-tauri/tauri.conf.json (当前构建配置)
    - package.json (构建脚本)
  </read_first>
  <action>
验证 `src-tauri/tauri.conf.json` 的 macOS 构建配置：

**检查项**:
1. `bundle.targets` 包含 "all" 或明确指定 macOS 目标
2. `bundle.icon` 包含 macOS 图标路径 (icon.icns)
3. `bundle.category` 设置正确 (应为 "Developer Tool" 或 "public.app-category.developer-tools")
4. `bundle.identifier` 格式正确 (反向域名，如 com.gsd.ui)
5. `bundle.shortDescription` 和 `longDescription` 已填写
6. `app.windows` 配置适合 macOS (devtools 在生产构建中应设为 false)

**需要更新的配置**:
```json
{
  "bundle": {
    "targets": ["app", "dmg"],  // 或保持 "all"
    "icon": ["icons/icon.icns"],
    "category": "public.app-category.developer-tools",
    "shortDescription": "A desktop interface for the GSD workflow",
    "longDescription": "GSD UI provides a visual interface for the GSD (Get Shit Done) workflow..."
  }
}
```

**验证 package.json**:
- `build` 脚本存在: `"build": "tsc && vite build"`
- `tauri:build` 脚本存在: `"tauri build"`

如果需要，更新配置使其符合 macOS 最佳实践。
  </action>
  <verify>
  <automated>cat src-tauri/tauri.conf.json | jq '.bundle.targets' && cat src-tauri/tauri.conf.json | jq '.bundle.category' | grep -q "developer"</automated>
  </verify>
  <done>
- tauri.conf.json 包含正确的 macOS 构建配置
- bundle.category 设置为 developer-tools
- bundle.icon 包含 .icns 文件
  </done>
</task>

<task type="auto">
  <name>Task 2: 执行 macOS 构建</name>
  <files>src-tauri/target/release/bundle/macos</files>
  <read_first>
    - package.json (确认构建脚本)
  </read_first>
  <action>
执行 macOS .app bundle 构建：

**构建命令**:
```bash
npm run tauri:build
```

**预期输出**:
- 构建过程会编译 Rust 代码
- 打包前端资源
- 创建 .app bundle 在 `src-tauri/target/release/bundle/macos/GSD UI.app`
- 可能创建 .dmg 安装包

**构建时间**:
- 首次构建: 5-15 分钟（需要编译 Rust 依赖）
- 后续构建: 2-5 分钟

**注意事项**:
- 确保系统已安装 Xcode 命令行工具 (`xcode-select --install`)
- 确保 Rust 工具链已安装 (`rustc --version`)
- 构建前会自动运行 `npm run build` (前端构建)

**验证构建产物**:
```bash
ls -la src-tauri/target/release/bundle/macos/
```

应该看到:
- `GSD UI.app` (应用程序包)
- `GSD UI {version}.dmg` (磁盘映像，可选)
  </action>
  <verify>
  <automated>test -d "src-tauri/target/release/bundle/macos/GSD UI.app" && echo "Build successful"</automated>
  </verify>
  <done>
- .app bundle 成功创建
- .app 大小合理（约 10-30 MB）
- 构建日志无严重错误
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>macOS .app bundle</what-built>
  <how-to-verify>
1. 双击 `src-tauri/target/release/bundle/macos/GSD UI.app` 启动应用
2. 验证应用正常启动（无崩溃）
3. 测试核心功能:
   - CLI 检测（如果已安装 claude）
   - 项目路径选择
   - 命令执行（Terminal View）
   - 文件浏览（Documents View）
   - 进度视图（Dashboard）
4. 检查应用图标在 Dock 和标题栏显示正确
5. 测试应用窗口调整大小和最小化/恢复
6. 验证菜单栏显示正确的应用名称
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述发现的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .app → System | 应用请求文件系统、进程执行权限 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | T (Tampering) | .app bundle | accept | macOS 代码签名在后续计划中处理 |
| T-05-03-02 | E (Elevation of Privilege) | App capabilities | mitigate | Tauri capabilities 系统限制权限范围 |
| T-05-03-03 | I (Information Disclosure) | App bundle | accept | 应用包含前端代码（可被检查，无敏感信息） |
</threat_model>

<verification>
1. 验证 .app 存在: `test -d "src-tauri/target/release/bundle/macos/GSD UI.app"`
2. 检查 .app 结构: `ls -R "src-tauri/target/release/bundle/macos/GSD UI.app/Contents"`
3. 验证应用版本: `defaults read $(pwd)/src-tauri/target/release/bundle/macos/GSD\ UI.app/Contents/Info.plist CFBundleShortVersionString`
</verification>

<success_criteria>
1. .app bundle 成功创建
2. 应用可以双击启动
3. 所有核心功能在 macOS 上正常工作
4. 应用图标正确显示
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-distribution/05-03-SUMMARY.md`
</output>
