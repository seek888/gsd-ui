---
phase: 6-electron
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - electron/main.ts
  - electron/preload.ts
  - index.html
autonomous: false
requirements: []
must_haves:
  truths:
    - "Electron 应用可以启动"
    - "Vite 开发服务器正常工作"
    - "React 应用在 Electron 窗口中渲染"
---

# Plan 06-01: Electron 项目脚手架

## 目标
搭建 Electron + React + Vite 项目结构，移除 Tauri 相关依赖。

## Tasks

### Task 1: 清理 Tauri 依赖
```bash
# 移除 Tauri 相关
npm uninstall @tauri-apps/api @tauri-apps/plugin-shell @tauri-apps/plugin-fs @tauri-apps/plugin-dialog @tauri-apps/plugin-store @tauri-apps/cli

# 删除 Tauri 目录
rm -rf src-tauri/
```

### Task 2: 安装 Electron 依赖
```bash
# 核心依赖
npm install electron electron-store electron-log

# 开发依赖
npm install -D electron-builder electron-vite vite-plugin-electron vite-plugin-electron-renderer concurrently wait-on
```

### Task 3: 配置 electron-vite
更新 `vite.config.ts`:
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import electron from 'vite-plugin-electron'
import renderer from 'vite-plugin-electron-renderer'
import { resolve } from 'path'

export default defineConfig({
  plugins: [
    react(),
    electron([
      {
        entry: 'electron/main.ts',
        onstart(args) {
          args.startup()
        },
        vite: {
          build: { outDir: 'dist-electron' }
        }
      },
      {
        entry: 'electron/preload.ts',
        onstart(args) {
          args.reload()
        },
        vite: {
          build: { outDir: 'dist-electron' }
        }
      }
    ]),
    renderer()
  ],
  resolve: {
    alias: { '@': resolve(__dirname, 'src') }
  }
})
```

### Task 4: 创建 Electron 入口
创建 `electron/main.ts`:
```typescript
import { app, BrowserWindow } from 'electron'
import { join } from 'path'

let mainWindow: BrowserWindow | null = null

function createWindow() {
  mainWindow = new BrowserWindow({
    width: 1200,
    height: 800,
    minWidth: 900,
    minHeight: 600,
    webPreferences: {
      preload: join(__dirname, 'preload.js'),
      nodeIntegration: false,
      contextIsolation: true
    },
    title: 'GSD UI'
  })

  if (process.env.VITE_DEV_SERVER_URL) {
    mainWindow.loadURL(process.env.VITE_DEV_SERVER_URL)
    mainWindow.webContents.openDevTools()
  } else {
    mainWindow.loadFile(join(__dirname, '../dist/index.html'))
  }
}

app.whenReady().then(createWindow)
app.on('window-all-closed', () => app.quit())
```

创建 `electron/preload.ts`:
```typescript
import { contextBridge } from 'electron'

contextBridge.exposeInMainWorld('electronAPI', {
  platform: process.platform
})
```

### Task 5: 更新 package.json scripts
```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "electron:dev": "vite",
    "electron:build": "npm run build && electron-builder",
    "electron:preview": "vite build && electron ."
  }
}
```

## 验证
- `npm run electron:dev` 启动 Electron 应用
- React 应用在窗口中正常渲染
- 开发者工具可以打开

## 下一步
06-02: 实现 IPC 通信和 preload API
