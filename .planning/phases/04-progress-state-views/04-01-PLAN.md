---
phase: 04-progress-state-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/ui/badge.tsx
  - src/components/ui/progress.tsx
  - src/components/ui/accordion.tsx
  - src/components/ui/separator.tsx
autonomous: true
requirements:
  - PROG-01
  - PROG-02

must_haves:
  truths:
    - "Badge 组件显示不同状态（已完成/进行中/未开始）"
    - "Progress 组件显示水平进度条"
    - "Accordion 组件支持展开/收起"
    - "Separator 组件提供视觉分隔"
  artifacts:
    - path: "src/components/ui/badge.tsx"
      provides: "Badge component for status indicators"
      exports: ["Badge"]
    - path: "src/components/ui/progress.tsx"
      provides: "Progress component for progress bars"
      exports: ["Progress"]
    - path: "src/components/ui/accordion.tsx"
      provides: "Accordion component for expandable phases"
      exports: ["Accordion", "AccordionItem", "AccordionTrigger", "AccordionContent"]
    - path: "src/components/ui/separator.tsx"
      provides: "Separator component for visual separation"
      exports: ["Separator"]
  key_links:
    - from: "src/components/ui/badge.tsx"
      to: "class-variance-authority"
      via: "import { cva } from 'class-variance-authority'"
      pattern: "cva\\("
    - from: "src/components/ui/progress.tsx"
      to: "@radix-ui/react-progress"
      via: "import * as Progress from '@radix-ui/react-progress'"
      pattern: "@radix-ui/react-progress"
    - from: "src/components/ui/accordion.tsx"
      to: "@radix-ui/react-accordion"
      via: "import * as Accordion from '@radix-ui/react-accordion'"
      pattern: "@radix-ui/react-accordion"
    - from: "src/components/ui/separator.tsx"
      to: "@radix-ui/react-separator"
      via: "import * as Separator from '@radix-ui/react-separator'"
      pattern: "@radix-ui/react-separator"
---

<objective>
添加 shadcn/ui 组件：Badge（状态徽章）、Progress（进度条）、Accordion（手风琴）、Separator（分隔符）

Purpose: 为 Phase 4 的进度视图提供必需的 UI 组件基础，这些组件用于显示阶段状态、进度百分比、展开/收起功能和视觉分隔。

Output: 四个新的 shadcn/ui 组件（Badge、Progress、Accordion、Separator）和对应的 Radix UI 依赖
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-progress-state-views/04-UI-SPEC.md
@.planning/phases/04-progress-state-views/04-CONTEXT.md
@.planning/phases/01-scaffold-foundation/01-CONTEXT.md
@src/components/ui/card.tsx
@src/components/ui/button.tsx
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 Radix UI 依赖包</name>
  <files>package.json</files>
  <read_first>
    - package.json
  </read_first>
  <action>
    使用 npm 安装以下 Radix UI 包：
    - @radix-ui/react-progress（进度条原始组件）
    - @radix-ui/react-accordion（手风琴原始组件）

    确认 @radix-ui/react-separator 已安装（Phase 1 中已安装，版本 ^1.1.2）

    命令：
    ```
    npm install @radix-ui/react-progress @radix-ui/react-accordion
    ```

    不要安装其他无关依赖。
  </action>
  <verify>
    <automated>grep -E "@radix-ui/react-progress|@radix-ui/react-accordion" package.json</automated>
  </verify>
  <done>
    - package.json 包含 @radix-ui/react-progress 和 @radix-ui/react-accordion
    - node_modules 中安装了这两个包
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 Badge 组件</name>
  <files>src/components/ui/badge.tsx</files>
  <read_first>
    - src/components/ui/button.tsx（参考 button 组件的 variant 模式）
    - src/lib/utils.ts（确认 cn 工具函数）
  </read_first>
  <action>
    创建 src/components/ui/badge.tsx，使用 class-variance-authority 定义 variant：

    ```typescript
    import * as React from "react"
    import { cva, type VariantProps } from "class-variance-authority"
    import { cn } from "@/lib/utils"

    const badgeVariants = cva(
      "inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold transition-colors focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2",
      {
        variants: {
          variant: {
            default:
              "border-transparent bg-primary text-primary-foreground hover:bg-primary/80",
            secondary:
              "border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80",
            destructive:
              "border-transparent bg-destructive text-destructive-foreground hover:bg-destructive/80",
            outline: "text-foreground",
            success:
              "border-transparent bg-green-500 text-white hover:bg-green-600",
            warning:
              "border-transparent bg-yellow-500 text-white hover:bg-yellow-600",
          },
        },
        defaultVariants: {
          variant: "default",
        },
      }
    )

    export interface BadgeProps
      extends React.HTMLAttributes<HTMLDivElement>,
        VariantProps<typeof badgeVariants> {}

    function Badge({ className, variant, ...props }: BadgeProps) {
      return (
        <div className={cn(badgeVariants({ variant }), className)} {...props} />
      )
    }

    export { Badge, badgeVariants }
    ```

    variant 属性：
    - default: 主要样式（primary 背景）
    - secondary: 次要样式
    - destructive: 错误样式（红色）
    - outline: 描边样式
    - success: 成功样式（绿色） - 用于"已完成"状态
    - warning: 警告样式（黄色） - 用于"进行中"状态
  </action>
  <verify>
    <automated>grep -q "export function Badge" src/components/ui/badge.tsx && grep -q "success:" src/components/ui/badge.tsx && grep -q "warning:" src/components/ui/badge.tsx</automated>
  </verify>
  <done>
    - Badge 组件已创建
    - 支持 6 种 variant：default、secondary、destructive、outline、success、warning
    - success 和 warning variant 用于阶段状态显示
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 Progress 组件</name>
  <files>src/components/ui/progress.tsx</files>
  <read_first>
    - src/components/ui/card.tsx（参考 forwardRef 模式）
    - src/lib/utils.ts
  </read_first>
  <action>
    创建 src/components/ui/progress.tsx，基于 @radix-ui/react-progress：

    ```typescript
    import * as React from "react"
    import * as ProgressPrimitive from "@radix-ui/react-progress"

    import { cn } from "@/lib/utils"

    const Progress = React.forwardRef<
      React.ElementRef<typeof ProgressPrimitive.Root>,
      React.ComponentPropsWithoutRef<typeof ProgressPrimitive.Root>
    >(({ className, value, ...props }, ref) => (
      <ProgressPrimitive.Root
        ref={ref}
        className={cn(
          "relative h-4 w-full overflow-hidden rounded-full bg-secondary",
          className
        )}
        {...props}
      >
        <ProgressPrimitive.Indicator
          className="h-full w-full flex-1 bg-primary transition-all"
          style={{ transform: `translateX(-${100 - (value || 0)}%)` }}
        />
      </ProgressPrimitive.Root>
    ))
    Progress.displayName = ProgressPrimitive.Root.displayName

    export { Progress }
    ```

    组件特性：
    - 使用 Radix UI Progress 原始组件
    - h-4 高度（16px）
    - 圆角全圆（rounded-full）
    - 背景色 bg-secondary（浅灰色）
    - 进度条颜色 bg-primary（主题色）
    - 支持过渡动画（transition-all）
    - value 属性传入 0-100 的百分比
  </action>
  <verify>
    <automated>grep -q "from \"@radix-ui/react-progress\"" src/components/ui/progress.tsx && grep -q "export.*Progress" src/components/ui/progress.tsx</automated>
  </verify>
  <done>
    - Progress 组件已创建
    - 基于 @radix-ui/react-progress
    - 接受 value 属性（0-100）
    - 支持过渡动画
  </done>
</task>

<task type="auto">
  <name>Task 4: 创建 Accordion 组件</name>
  <files>src/components/ui/accordion.tsx</files>
  <read_first>
    - src/components/ui/card.tsx（参考 forwardRef 模式）
    - src/lib/utils.ts
  </read_first>
  <action>
    创建 src/components/ui/accordion.tsx，基于 @radix-ui/react-accordion：

    ```typescript
    import * as React from "react"
    import * as AccordionPrimitive from "@radix-ui/react-accordion"
    import { ChevronDown } from "lucide-react"

    import { cn } from "@/lib/utils"

    const Accordion = AccordionPrimitive.Root

    const AccordionItem = React.forwardRef<
      React.ElementRef<typeof AccordionPrimitive.Item>,
      React.ComponentPropsWithoutRef<typeof AccordionPrimitive.Item>
    >(({ className, ...props }, ref) => (
      <AccordionPrimitive.Item
        ref={ref}
        className={cn("border-b", className)}
        {...props}
      />
    ))
    AccordionItem.displayName = "AccordionItem"

    const AccordionTrigger = React.forwardRef<
      React.ElementRef<typeof AccordionPrimitive.Trigger>,
      React.ComponentPropsWithoutRef<typeof AccordionPrimitive.Trigger>
    >(({ className, children, ...props }, ref) => (
      <AccordionPrimitive.Trigger
        ref={ref}
        className={cn(
          "flex flex-1 items-center justify-between py-4 font-medium transition-all hover:underline [&[data-state=open]>svg]:rotate-180",
          className
        )}
        {...props}
      >
        {children}
        <ChevronDown className="h-4 w-4 shrink-0 transition-transform duration-200" />
      </AccordionPrimitive.Trigger>
    ))
    AccordionTrigger.displayName = AccordionPrimitive.Trigger.displayName

    const AccordionContent = React.forwardRef<
      React.ElementRef<typeof AccordionPrimitive.Content>,
      React.ComponentPropsWithoutRef<typeof AccordionPrimitive.Content>
    >(({ className, children, ...props }, ref) => (
      <AccordionPrimitive.Content
        ref={ref}
        className="overflow-hidden text-sm transition-all data-[state=closed]:animate-accordion-up data-[state=open]:animate-accordion-down"
        {...props}
      >
        <div className={cn("pb-4 pt-0", className)}>{children}</div>
      </AccordionPrimitive.Content>
    ))
    AccordionContent.displayName = AccordionPrimitive.Content.displayName

    export { Accordion, AccordionItem, AccordionTrigger, AccordionContent }
    ```

    组件特性：
    - Accordion: 根容器，支持 type="single|multiple" 属性
    - AccordionItem: 每个手风琴项，带底部边框
    - AccordionTrigger: 触发按钮，右侧有旋转的 ChevronDown 图标
    - AccordionContent: 内容区域，带展开/收起动画
    - 使用 data-state 属性控制旋转和动画
  </action>
  <verify>
    <automated>grep -q "from \"@radix-ui/react-accordion\"" src/components/ui/accordion.tsx && grep -q "export.*Accordion" src/components/ui/accordion.tsx</automated>
  </verify>
  <done>
    - Accordion 组件已创建
    - 导出 Accordion、AccordionItem、AccordionTrigger、AccordionContent
    - 支持展开/收起动画
    - ChevronDown 图标旋转指示状态
  </done>
</task>

<task type="auto">
  <name>Task 5: 创建 Separator 组件</name>
  <files>src/components/ui/separator.tsx</files>
  <read_first>
    - src/components/ui/card.tsx（参考 forwardRef 模式）
    - src/lib/utils.ts
  </read_first>
  <action>
    创建 src/components/ui/separator.tsx，基于 @radix-ui/react-separator（已在 Phase 1 安装）：

    ```typescript
    import * as React from "react"
    import * as SeparatorPrimitive from "@radix-ui/react-separator"

    import { cn } from "@/lib/utils"

    const Separator = React.forwardRef<
      React.ElementRef<typeof SeparatorPrimitive.Root>,
      React.ComponentPropsWithoutRef<typeof SeparatorPrimitive.Root>
    >(
      (
        { className, orientation = "horizontal", decorative = true, ...props },
        ref
      ) => (
        <SeparatorPrimitive.Root
          ref={ref}
          decorative={decorative}
          orientation={orientation}
          className={cn(
            "shrink-0 bg-border",
            orientation === "horizontal" ? "h-[1px] w-full" : "h-full w-[1px]",
            className
          )}
          {...props}
        />
      )
    )
    Separator.displayName = SeparatorPrimitive.Root.displayName

    export { Separator }
    ```

    组件特性：
    - 支持 orientation="horizontal|vertical"（默认 horizontal）
    - horizontal: h-[1px] w-full（1px 高的横线）
    - vertical: h-full w-[1px]（1px 宽的竖线）
    - 颜色 bg-border（主题边框色）
    - decorative=true 表示纯装饰性（屏幕阅读器忽略）
  </action>
  <verify>
    <automated>grep -q "from \"@radix-ui/react-separator\"" src/components/ui/separator.tsx && grep -q "export.*Separator" src/components/ui/separator.tsx</automated>
  </verify>
  <done>
    - Separator 组件已创建
    - 支持水平和垂直方向
    - 用于阶段列表和会话上下文之间的视觉分隔
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | 包管理器执行网络下载，可能引入恶意依赖 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | S | npm packages (Radix UI) | mitigate | 仅使用官方 @radix-ui 包，这些是经过验证的可信包。已存在 package.json 中的其他 radix 包证明了信任链。 |
| T-04-02 | T | npm packages | accept | 无用户数据传输，纯前端组件 |
| T-04-03 | R | component code | accept | 仅渲染 UI，无数据访问 |
| T-04-04 | I | component code | accept | 无外部输入处理 |
| T-04-05 | D | component code | accept | 无数据存储 |
| T-04-06 | E | component code | accept | 无权限提升可能 |
</threat_model>

<verification>
- [ ] 所有新组件已创建
- [ ] Badge 组件支持 success/warning variant
- [ ] Progress 组件接受 value 属性
- [ ] Accordion 组件支持多级展开
- [ ] Separator 组件支持水平/垂直方向
- [ ] npm install 成功无错误
- [ ] 组件与现有 UI 系统风格一致
</verification>

<success_criteria>
- [ ] npm install 完成后 package.json 包含新依赖
- [ ] 所有四个组件文件存在且导出正确
- [ ] 组件可被其他组件导入使用
- [ ] TypeScript 类型检查通过
</success_criteria>

<output>
After completion, create `.planning/phases/04-progress-state-views/04-01-SUMMARY.md`
</output>
