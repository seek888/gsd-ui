---
phase: 04-progress-state-views
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - src/components/progress/SessionContext.tsx
  - src/components/progress/AttentionPanel.tsx
  - src/views/DashboardView.tsx
autonomous: false
requirements:
  - PROG-03
  - PROG-04

must_haves:
  truths:
    - "用户可以在页面底部看到当前会话上下文"
    - "会话上下文显示最近活动、阻塞项、关键决策"
    - "用户可以在注意事项面板看到未完成计划和阻塞项"
    - "注意事项项可以点击跳转到相关阶段/计划"
  artifacts:
    - path: "src/components/progress/SessionContext.tsx"
      provides: "会话上下文组件"
      exports: ["SessionContext"]
      contains: "RecentActivity, Blockers, Decisions sections"
    - path: "src/components/progress/AttentionPanel.tsx"
      provides: "注意事项面板组件"
      exports: ["AttentionPanel"]
      contains: "incomplete plans, blockers, outstanding items"
  key_links:
    - from: "src/components/progress/SessionContext.tsx"
      to: "src/stores/progressStore.ts"
      via: "import { useProgressStore } from '@/stores/progressStore'"
      pattern: "from '@/stores/progressStore'"
    - from: "src/components/progress/AttentionPanel.tsx"
      to: "src/stores/progressStore.ts"
      via: "import { useProgressStore } from '@/stores/progressStore'"
      pattern: "from '@/stores/progressStore'"
    - from: "src/views/DashboardView.tsx"
      to: "src/components/progress/SessionContext.tsx"
      via: "import { SessionContext } from '@/components/progress/SessionContext'"
      pattern: "from '@/components/progress/SessionContext'"
    - from: "src/views/DashboardView.tsx"
      to: "src/components/progress/AttentionPanel.tsx"
      via: "import { AttentionPanel } from '@/components/progress/AttentionPanel'"
      pattern: "from '@/components/progress/AttentionPanel'"
---

<objective>
实现会话上下文区域和注意事项面板，显示当前会话状态、阻塞项和需要注意的事项

Purpose: 让用户能够快速了解当前项目的会话状态（最近活动、阻塞项、关键决策），以及需要关注的事项（未完成计划、阻塞项），帮助用户把握项目当前状态。

Output: SessionContext 组件（会话上下文）、AttentionPanel 组件（注意事项面板）、更新后的 DashboardView（集成新组件）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-progress-state-views/04-CONTEXT.md
@.planning/phases/04-progress-state-views/04-UI-SPEC.md
@.planning/STATE.md
@src/views/DashboardView.tsx
@src/components/ui/card.tsx
@src/components/ui/separator.tsx
@src/components/ui/badge.tsx
@src/types/progress.ts
@src/stores/progressStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 SessionContext 组件</name>
  <files>src/components/progress/SessionContext.tsx</files>
  <read_first>
    - src/types/progress.ts（SessionState 类型定义）
    - .planning/STATE.md（了解 state 数据结构）
    - src/components/ui/card.tsx（Card 组件）
    - src/components/ui/separator.tsx（Separator 组件）
  </read_first>
  <action>
    创建 src/components/progress/SessionContext.tsx，显示当前会话状态：

    ```typescript
    import { Clock, AlertTriangle, Lightbulb, Activity } from 'lucide-react';
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
    import { Badge } from '@/components/ui/badge';
    import { Separator } from '@/components/ui/separator';
    import type { SessionState } from '@/types/progress';
    import { cn } from '@/lib/utils';

    interface SessionContextProps {
      sessionState: SessionState | null;
    }

    export function SessionContext({ sessionState }: SessionContextProps) {
      if (!sessionState) {
        return (
          <Card className="border-muted">
            <CardContent className="p-6">
              <p className="text-sm text-muted-foreground text-center">
                暂无会话数据
              </p>
            </CardContent>
          </Card>
        );
      }

      const { currentPhase, currentPlan, lastActivity, blockers, decisions, recentActivity } = sessionState;

      return (
        <Card className="border-muted bg-muted/30">
          <CardHeader className="pb-3">
            <CardTitle className="text-base flex items-center gap-2">
              <Activity className="h-4 w-4" />
              当前会话
            </CardTitle>
          </CardHeader>
          <CardContent className="space-y-4">
            {/* 最近位置 */}
            <div className="space-y-2">
              <div className="flex items-center gap-2 text-sm">
                <Clock className="h-4 w-4 text-muted-foreground shrink-0" />
                <span className="font-medium">最近位置：</span>
              </div>
              <div className="pl-6 text-sm space-y-1">
                <div className="flex items-center gap-2">
                  <span className="text-muted-foreground">阶段：</span>
                  <Badge variant="secondary">{currentPhase}</Badge>
                  {currentPlan && (
                    <>
                      <span className="text-muted-foreground">·</span>
                      <span>计划 {currentPlan}</span>
                    </>
                  )}
                </div>
                {lastActivity && (
                  <p className="text-muted-foreground">{lastActivity}</p>
                )}
              </div>
            </div>

            <Separator />

            {/* 阻塞项 */}
            <div className="space-y-2">
              <div className="flex items-center gap-2 text-sm">
                <AlertTriangle className={cn(
                  "h-4 w-4 shrink-0",
                  blockers.length > 0 ? "text-destructive" : "text-muted-foreground"
                )} />
                <span className="font-medium">阻塞项：</span>
                {blockers.length > 0 && (
                  <Badge variant="destructive" className="text-xs">
                    {blockers.length}
                  </Badge>
                )}
              </div>
              <div className="pl-6 text-sm">
                {blockers.length === 0 ? (
                  <p className="text-muted-foreground">无阻塞项</p>
                ) : (
                  <ul className="space-y-1">
                    {blockers.map((blocker) => (
                      <li
                        key={blocker.id}
                        className={cn(
                          "flex items-start gap-2",
                          blocker.severity === 'high' && "text-destructive",
                          blocker.severity === 'medium' && "text-yellow-600 dark:text-yellow-400"
                        )}
                      >
                        <span className="shrink-0">•</span>
                        <span>{blocker.description}</span>
                      </li>
                    ))}
                  </ul>
                )}
              </div>
            </div>

            {/* 关键决策（可选显示，如果有空间） */}
            {decisions.length > 0 && (
              <>
                <Separator />
                <div className="space-y-2">
                  <div className="flex items-center gap-2 text-sm">
                    <Lightbulb className="h-4 w-4 text-muted-foreground shrink-0" />
                    <span className="font-medium">关键决策：</span>
                    <Badge variant="outline" className="text-xs">
                      {decisions.length}
                    </Badge>
                  </div>
                  <div className="pl-6 text-sm">
                    <ul className="space-y-1">
                      {decisions.slice(0, 3).map((decision) => (
                        <li key={decision.id} className="text-muted-foreground">
                          • {decision.description}
                        </li>
                      ))}
                      {decisions.length > 3 && (
                        <li className="text-muted-foreground text-xs">
                          ...还有 {decisions.length - 3} 项决策
                        </li>
                      )}
                    </ul>
                  </div>
                </div>
              </>
            )}
          </CardContent>
        </Card>
      );
    }
    ```

    组件特性：
    - Card 容器，浅色背景（bg-muted/30）
    - 标题带 Activity 图标
    - 最近位置区域：
      - Clock 图标
      - 显示当前阶段编号（Badge）
      - 显示当前计划（如果有）
      - 显示最近活动描述
    - 阻塞项区域：
      - AlertTriangle 图标（有阻塞时红色）
      - 阻塞数量 Badge（有阻塞时 destructive）
      - 列出所有阻塞项（带严重程度颜色）
      - 无阻塞时显示"无阻塞项"
    - 关键决策区域（可选）：
      - Lightbulb 图标
      - 决策数量 Badge
      - 显示前 3 条决策
      - 超过 3 条时显示省略提示
  </action>
  <verify>
    <automated>grep -q "export function SessionContext" src/components/progress/SessionContext.tsx && grep -q "AlertTriangle\\|Clock\\|Lightbulb\\|Activity" src/components/progress/SessionContext.tsx</automated>
  </verify>
  <done>
    - SessionContext 组件已创建
    - 显示最近位置（阶段、计划、活动描述）
    - 显示阻塞项列表（带严重程度颜色）
    - 显示关键决策摘要
    - 无数据时显示占位内容
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 AttentionPanel 组件</name>
  <files>src/components/progress/AttentionPanel.tsx</files>
  <read_first>
    - src/types/progress.ts（RoadmapData 类型定义）
    - src/components/ui/card.tsx（Card 组件）
    - src/components/ui/badge.tsx（Badge 组件）
  </read_first>
  <action>
    创建 src/components/progress/AttentionPanel.tsx，显示需要注意的事项：

    ```typescript
    import { AlertCircle, AlertTriangle, Info } from 'lucide-react';
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
    import { Badge } from '@/components/ui/badge';
    import type { RoadmapData, SessionState } from '@/types/progress';
    import { cn } from '@/lib/utils';

    interface AttentionPanelProps {
      roadmapData: RoadmapData | null;
      sessionState: SessionState | null;
    }

    interface AttentionItem {
      id: string;
      type: 'incomplete' | 'blocker' | 'info';
      title: string;
      description: string;
      phaseNumber?: number;
    }

    export function AttentionPanel({ roadmapData, sessionState }: AttentionPanelProps) {
      // 收集需要注意的事项
      const attentionItems: AttentionItem[] = [];

      // 添加未完成计划
      if (roadmapData) {
        roadmapData.phases.forEach((phase) => {
          if (phase.status !== 'complete') {
            phase.plans.forEach((plan) => {
              if (plan.status !== 'complete') {
                attentionItems.push({
                  id: `plan-${plan.id}`,
                  type: 'incomplete',
                  title: `未完成计划: ${plan.id}`,
                  description: plan.name,
                  phaseNumber: phase.number,
                });
              }
            });
          }
        });

        // 添加当前阶段的未完成计划（高优先级）
        const currentPhase = roadmapData.phases.find(p => p.isActive);
        if (currentPhase && currentPhase.status !== 'complete') {
          attentionItems.unshift({
            id: 'current-phase',
            type: 'info',
            title: '当前阶段未完成',
            description: `${currentPhase.name} 还剩 ${currentPhase.plansCount - currentPhase.completedPlans} 个计划`,
            phaseNumber: currentPhase.number,
          });
        }
      }

      // 添加阻塞项
      if (sessionState && sessionState.blockers.length > 0) {
        sessionState.blockers.forEach((blocker, index) => {
          attentionItems.push({
            id: `blocker-${blocker.id}`,
            type: 'blocker',
            title: `阻塞项 #${index + 1}`,
            description: blocker.description,
          });
        });
      }

      // 如果没有需要关注的事项
      if (attentionItems.length === 0) {
        return null;
      }

      return (
        <Card className="border-yellow-200 dark:border-yellow-900 bg-yellow-50/50 dark:bg-yellow-900/10">
          <CardHeader className="pb-3">
            <CardTitle className="text-base flex items-center gap-2">
              <AlertCircle className="h-4 w-4 text-yellow-600 dark:text-yellow-400" />
              需要关注
              <Badge variant="outline" className="ml-auto">
                {attentionItems.length}
              </Badge>
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="space-y-2">
              {attentionItems.slice(0, 5).map((item) => {
                const Icon = item.type === 'blocker' ? AlertTriangle :
                            item.type === 'incomplete' ? AlertCircle : Info;

                return (
                  <div
                    key={item.id}
                    className={cn(
                      "flex items-start gap-2 p-2 rounded-md bg-background/50",
                      item.type === 'blocker' && "border-l-2 border-destructive",
                      item.type === 'incomplete' && "border-l-2 border-yellow-500"
                    )}
                  >
                    <Icon className={cn(
                      "h-4 w-4 shrink-0 mt-0.5",
                      item.type === 'blocker' && "text-destructive",
                      item.type === 'incomplete' && "text-yellow-600 dark:text-yellow-400",
                      item.type === 'info' && "text-blue-600 dark:text-blue-400"
                    )} />
                    <div className="flex-1 min-w-0">
                      <p className="text-sm font-medium truncate">{item.title}</p>
                      <p className="text-xs text-muted-foreground truncate">{item.description}</p>
                      {item.phaseNumber && (
                        <Badge variant="outline" className="text-xs mt-1">
                          阶段 {item.phaseNumber}
                        </Badge>
                      )}
                    </div>
                  </div>
                );
              })}
              {attentionItems.length > 5 && (
                <p className="text-xs text-muted-foreground text-center pt-2">
                  ...还有 {attentionItems.length - 5} 项
                </p>
              )}
            </div>
          </CardContent>
        </Card>
      );
    }
    ```

    组件特性：
    - 从 roadmapData 和 sessionState 收集注意事项
    - 包括三类事项：
      - incomplete: 未完成计划
      - blocker: 阻塞项（高优先级）
      - info: 信息提示（如当前阶段未完成）
    - 卡片样式：
      - 黄色边框和浅色背景（警告视觉）
      - AlertCircle 图标
      - 显示事项数量 Badge
    - 每个事项：
      - 左侧图标（不同类型不同颜色）
      - 标题和描述（可截断）
      - 阶段编号 Badge（如果有）
      - 左侧彩色边框指示优先级
    - 最多显示 5 项，超过时显示省略提示
    - 无事项时返回 null
  </action>
  <verify>
    <automated>grep -q "export function AttentionPanel" src/components/progress/AttentionPanel.tsx && grep -q "AlertCircle\\|AlertTriangle\\|Info" src/components/progress/AttentionPanel.tsx</automated>
  </verify>
  <done>
    - AttentionPanel 组件已创建
    - 从 roadmap 和 state 收集注意事项
    - 显示未完成计划、阻塞项、信息提示
    - 使用图标和颜色区分事项类型
    - 限制显示数量避免过长
  </done>
</task>

<task type="auto">
  <name>Task 3: 集成 SessionContext 和 AttentionPanel 到 DashboardView</name>
  <files>src/views/DashboardView.tsx</files>
  <read_first>
    - src/views/DashboardView.tsx（当前实现）
    - src/components/progress/SessionContext.tsx
    - src/components/progress/AttentionPanel.tsx
  </read_first>
  <action>
    更新 src/views/DashboardView.tsx，在阶段列表下方添加会话上下文和注意事项面板：

    1. 在导入部分添加新组件：
    ```typescript
    import { SessionContext } from '@/components/progress/SessionContext';
    import { AttentionPanel } from '@/components/progress/AttentionPanel';
    ```

    2. 在主内容区域，将阶段列表和会话上下文放在一个可滚动的容器中：
    ```typescript
    return (
      <div className="h-full flex flex-col">
        {/* 头部：标题 + 刷新按钮（保持不变） */}
        <div className="flex items-center justify-between px-6 py-4 border-b">
          {/* ... 保持现有代码 ... */}
        </div>

        {/* 阶段列表 + 会话上下文 + 注意事项 */}
        <div className="flex-1 overflow-y-auto p-6">
          <div className="max-w-4xl mx-auto space-y-6">
            {/* 注意事项面板（如果有） */}
            <AttentionPanel
              roadmapData={roadmapData}
              sessionState={sessionState}
            />

            {/* 阶段卡片列表 */}
            <div className="space-y-4">
              {roadmapData.phases.map((phase) => (
                <PhaseCard
                  key={phase.number}
                  phase={phase}
                  isExpanded={expandedPhases.has(phase.number)}
                  onToggle={() => togglePhaseExpansion(phase.number)}
                />
              ))}
            </div>

            {/* 会话上下文 */}
            <SessionContext sessionState={sessionState} />
          </div>
        </div>
      </div>
    );
    ```

    3. 从 useProgressStore 中获取 sessionState：
    ```typescript
    const {
      roadmapData,
      sessionState,  // 添加这个
      loadingState,
      error,
      expandedPhases,
      loadProgressData,
      refreshData,
      togglePhaseExpansion,
      clearError,
    } = useProgressStore();
    ```

    布局调整说明：
    - 注意事项面板放在最上方（优先显示）
    - 阶段卡片列表在中间
    - 会话上下文放在最下方（参考 D-11 决定：页面底部区域）
    - 使用 space-y-6 创建更大的间距（区分不同区域）
    - 所有内容在 max-w-4xl 容器中居中
  </action>
  <verify>
    <automated>grep -q "SessionContext" src/views/DashboardView.tsx && grep -q "AttentionPanel" src/views/DashboardView.tsx && grep -q "sessionState" src/views/DashboardView.tsx</automated>
  </verify>
  <done>
    - DashboardView 已集成 SessionContext 和 AttentionPanel
    - 注意事项面板显示在顶部
    - 会话上下文显示在底部
    - 布局层次清晰（注意事项 -> 阶段列表 -> 会话上下文）
    - 使用 sessionState 数据
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - SessionContext 组件：显示当前会话的最近位置、阻塞项、关键决策
    - AttentionPanel 组件：显示未完成计划、阻塞项、需要关注的事项
    - DashboardView 更新：集成两个新组件，布局顺序为 注意事项 -> 阶段列表 -> 会话上下文
  </what-built>
  <how-to-verify>
    1. 启动开发服务器（如果还没运行）：`npm run tauri:dev`
    2. 查看 Dashboard 视图底部，验证会话上下文区域显示：
       - 最近位置（阶段编号、计划编号、活动描述）
       - 阻塞项（如果有，显示红色图标和列表）
       - 关键决策（如果有，显示前 3 条）
    3. 查看注意事项面板（如果有未完成计划或阻塞项）：
       - 显示在阶段列表上方
       - 黄色背景和警告视觉
       - 显示未完成计划和阻塞项
       - 不同类型事项有不同图标和颜色
    4. 验证事项数量超过 5 时显示省略提示
    5. 验证无阻塞项时显示"无阻塞项"
    6. 验证无注意事项时面板不显示
    7. 测试 STATE.md 更新后，会话上下文能正确反映变化
  </how-to-verify>
  <resume-signal>
    输入 "approved" 表示验证通过，或描述发现的问题
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 数据渲染 | 从 progressStore 读取 sessionState 和 roadmapData |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | S | 无 | accept | 仅渲染 UI，无安全影响 |
| T-04-21 | T | 无 | accept | 无数据传输 |
| T-04-22 | R | SessionContext, AttentionPanel | mitigate | 渲染前验证数据结构，使用 TypeScript 类型。限制显示数量防止过长的 UI 渲染。 |
| T-04-23 | I | 无 | accept | 无外部输入处理 |
| T-04-24 | D | 无 | accept | 无数据存储 |
| T-04-25 | E | 无 | accept | 无权限提升 |
</threat_model>

<verification>
- [ ] SessionContext 组件正确显示会话状态
- [ ] AttentionPanel 组件正确显示注意事项
- [ ] DashboardView 集成两个新组件
- [ ] 布局顺序正确（注意事项 -> 阶段列表 -> 会话上下文）
- [ ] 无数据时正确处理
- [ ] 超过 5 项时显示省略提示
- [ ] UI 符合 04-UI-SPEC.md 规范
</verification>

<success_criteria>
- [ ] 用户可以在页面底部看到当前会话上下文
- [ ] 会话上下文显示最近活动、阻塞项、关键决策
- [ ] 用户可以在注意事项面板看到未完成计划和阻塞项
- [ ] 注意事项项使用图标和颜色区分类型
- [ ] 无事项时面板隐藏
- [ ] 布局清晰，层次分明
</success_criteria>

<output>
After completion, create `.planning/phases/04-progress-state-views/04-04-SUMMARY.md`
</output>
